<?php

declare(strict_types=1);

namespace App\Check\ClosureThisCapture;

class C
{
    public string $name = 'x';

    public function run(): string
    {
        $f = function <T>(T $x): string {
            return $this->name;
        };

        return $f::<int>(1);
    }
}
