<?php

declare(strict_types=1);

namespace App\ConstructorMixed;

// Rejected: the allowed bare leading `T` must not stop the walk from reaching the
// bad trailing `?T` (a non-bare shape) — the inner-variance check still rejects it.
class P<+T>
{
    public function __construct(T $a, ?T $b)
    {
    }
}
