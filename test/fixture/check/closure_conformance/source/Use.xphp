<?php

declare(strict_types=1);

namespace App\ClosureCheck;

// The returned literal declares a `string` parameter where the target
// guarantees only `int` — a provable contravariance violation that
// `xphp check` must collect (not throw).
function make(): Closure(int $x): int
{
    return fn(string $x): int => 0;
}
