<?php

declare(strict_types=1);

namespace App\OkBounds;

use App\Other\Real;

// Clean: built-in bound (Stringable), imported bound (Real), a parameter
// referencing an earlier parameter as its default (B = A) and as its bound
// (C: A), and a multi-segment (namespaced) bound (Sub\Thing) — none are
// single-segment strays, so none are flagged.
class Ok<T: \Stringable, K: Real, A, C: A, D: Sub\Thing, B = A>
{
}
