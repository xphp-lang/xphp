<?php

declare(strict_types=1);

namespace App\Check\ParseError;

// xphp-specific parse-time rejection (not a PHP syntax error): variance markers
// are not allowed on closures. Throws a RuntimeException during parse, with no line.
$f = function <out T>(T $x): T {
    return $x;
};
