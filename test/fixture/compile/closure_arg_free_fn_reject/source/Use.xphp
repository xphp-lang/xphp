<?php

declare(strict_types=1);

namespace App\ClosureArgFnReject;

function apply<R>(Closure(R $x): R $fn, R $seed): R
{
    return $fn($seed);
}

// string is not wider than int — compile fails fast before any output is written.
apply::<int>(fn(string $x): int => 0, 5);
