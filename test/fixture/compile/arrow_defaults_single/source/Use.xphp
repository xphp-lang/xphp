<?php

declare(strict_types=1);

namespace App\ArrowDefaultsSingle;

// Single defaulted type param on an arrow. Empty turbofish `$f::<>(...)`
// pads to the default, yielding a specialization on T=int.
$f = fn<T = int>(T $x): T => $x;
$r = $f::<>(7);
